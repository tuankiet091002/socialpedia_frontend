export const ChatTitle = () => {
    // const {channelId} = useParams();
    //
    // const {data} = useGetOneChannelQuery(+(channelId as string));
    //
    // if (!data) return null;

    return (<div>
        Channel name here
    </div>)
}